import { HariFilterPipe } from './hari-filter.pipe';

describe('HariFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HariFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
