import { ProdiFilterPipe } from './pipe/prodi-filter.pipe';

describe('ProdiFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProdiFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
